# CLAUDE.md

This directory contains the Swift implementation of the Universal Test Suite (UTS) for LiveObjects.

The canonical source of documentation on the UTS is the `uts` directory in the `specification` repo. If you do not know where to find the `specification` repo, ask the user.

The current document describes how to adapt the general principles expressed in the UTS documentation to the Swift LiveObjects codebase.
